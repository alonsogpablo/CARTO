# CARTO
BATCH QUERY: si tenemos que extraer una tabla como resultado debemos hacer un SELECT INTO TABLA, y luego hacer otra query con un SELECT * FROM TABLA

En queries de larga duración tendremos que hacer un STATUS CHECK vía CURL conociendo el JOB ID
